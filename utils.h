#include <vector>
#include <string>

/** @brief  Split a string by the given character
 *  @param  str [IN] - text line
 *  @param  dev [IN] - divider
 *  @return Vector strings separated by a given character */
std::vector<std::string> split(const std::string &str, char div);

